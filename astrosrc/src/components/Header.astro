---
import HeaderLink from "./HeaderLink.astro";
import { SITE_TITLE, SITE_NAV } from "../consts";
import { Header } from "./Header.jsx";
const { pathname } = Astro.url;
---

<Header pathname={pathname} navigation={SITE_NAV} />
