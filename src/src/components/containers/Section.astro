---
interface Props {
  bg?: 'default' | 'alt'
  id?: string
  ariaLabel?: string
  class?: string
}

const { bg = 'default', id, ariaLabel, class: className } = Astro.props

const bgStyle = bg === 'alt' ? 'background: var(--color-bg-alt)' : 'background: var(--color-bg)'
---

<section
  style={bgStyle}
  class:list={['py-6 md:py-10', className]}
  {...id ? { id } : {}}
  {...ariaLabel ? { 'aria-label': ariaLabel } : {}}
>
  <slot />
</section>
